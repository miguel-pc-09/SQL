  -- Trae el maximo de años de la tabla usuarios. Traera la edad maxima de los usuarios 
SELECT MAX(age) FROM users;
     
  -- Trae el minimo de años de la tabla usuarios. Traera la edad minima de los usuarios
SELECT MIN(age) FROM users;
